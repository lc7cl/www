#include "cmd.h"

static cmdinfo_t **cmd;

int register_cmd(cmdinfo_t *cmd)
{
    return 0;
}

int unregister_cmd(cmdinfo_t *cmd)
{
    return 0;
}

cmdinfo_t * get_cmd(const char *name)
{

    return NULL;
}
